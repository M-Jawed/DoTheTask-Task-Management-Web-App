{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build",
    "watchPatterns": ["dothetask/backend/**"]
  },
  "deploy": {
    "startCommand": "npm run start",
    "workingDirectory": "dothetask/backend"
  }
}
